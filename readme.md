How to run
    1. if you need start from begin -> 
        a) pip install -r requirements.txt
        b) python manage.py makemigrations
        c) python manage.py migrate, then
        d) python manage.py runserver

    2. What are the tasks ? Linear Feedback Shift Register and API Werehouse (reporting)